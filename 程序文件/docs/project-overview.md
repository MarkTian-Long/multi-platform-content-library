# 多平台资料库

多平台资料库是一个 Windows 本地应用：把用户主动提交的公开链接或分享文案保存成可离线阅读、可搜索、可追溯的资料。它保存已经能够取得的内容，不尝试绕过登录、付费、验证码、DRM 或平台访问限制。

## 日常流程

```text
采集任务：粘贴链接 → 保存链接 → 查看任务状态
                                  ↓
                     缺少工具 / 需要登录时处理
                                  ↓
资料库：搜索、阅读、打开媒体或导出资料包
```

“登录与依赖”不是另一个采集入口。任务显示“缺少工具”时，先检查并安装本机工具；显示“需要登录”时，选择对应平台并在专用浏览器登录，然后回到采集任务重试。

## 支持范围

| 来源 | 可处理范围 | 已知边界 |
| --- | --- | --- |
| 微信公众号 | 正文、可取得图片、可正常下载的视频、离线页和 PDF | 受限、过期、分段流或超限视频会保留来源而不伪报下载成功 |
| B 站 | 公开视频、可用字幕和相关派生产物 | 可能需要登录；多 P 与不同账号状态仍需按单条任务结果判断 |
| 小红书 | 可访问笔记正文和媒体 | 登录、验证码和页面变化会影响结果 |
| 普通网页 | 可读取正文、图片及页面公开提供的资源 | 站点结构和媒体授权决定可保存范围 |

公开网页、微信公众号和一个公开 B 站视频已有实际保存验证。小红书、登录态和 B 站多 P 的真实链接覆盖仍在扩充，不能据此推断所有链接都可下载。

## 本地数据与隐私

- `资料库/`：新的多平台资料及派生产物。
- `文章库/`：原微信采集兼容存储；不会在整理或重试时被删除。
- `程序文件/`：程序、离线工具、模型、日志和文档。

资料、转写和 OCR 默认在本机处理；应用不读取默认浏览器资料、不监听剪贴板，也不调用云端 AI API。平台登录只使用应用专用浏览器资料。

## 维护

在 `程序文件/` 运行：

```powershell
cmd /c npm run build
cmd /c npm test
cmd /c npm run typecheck
node dist/link-cli.js doctor
```

更多操作和限制见操作手册 `link-library-manual.md`；本地 MCP 配置见 `mcp-setup.md`。

# 工作区清理记录

审计日期：2026-09-19。此次整理只处理重复的日常入口和对应说明，没有删除、重命名或移动真实文章、资料库、模型、依赖、日志或无法确认用途的文件。

## 已完成

- 根目录现在只保留 `启动多平台资料库.cmd` 作为日常入口。
- 原 `启动公众号文章阅读器.cmd` 已可逆移动到 `程序文件/legacy/启动公众号文章阅读器.cmd`。启动器已改为从该目录定位 `程序文件/reader-window.ps1`，并把项目根传给 `-ProjectRoot`，因此仍使用根目录 `文章库/`。
- `README.md` 已改为新用户先使用多平台资料库，并保留旧微信阅读器、兼容存储和原文章库说明。

## 根目录类别与容量（约值）

| 项目 | 类别 | 容量 |
| --- | --- | ---: |
| `程序文件/` | 程序、依赖、工具、模型、日志、测试和文档 | 1.69 GiB |
| `资料库/` | 新多平台资料及其派生产物 | 1.48 GiB |
| `.git/` | Git 历史 | 0.41 GiB |
| `文章库/` | 旧微信文章及兼容读取结果 | 0.16 GiB |

`程序文件/` 内部较大的类别是 `tools/`（约 701 MiB）、`logs/`（约 525 MiB）、`.venv-content/`（约 271 MiB）和 `.wechat-reader-profile/`（约 157 MiB）。这些目录分别承载处理工具、运行证据/日志、项目专用 Python 环境和专用浏览器资料，不能依据容量判定为冗余。

## 未清理项目

基于本次审计，没有清理 `文章库/`、`资料库/`、`程序文件/tools/`、`程序文件/.venv-content/`、`程序文件/.wechat-reader-profile/`、`程序文件/logs/`、`程序文件/node_modules/`、构建产物或未知文件。它们分别承担用户资料、可复用依赖、模型/工具、登录态、验收证据、恢复材料或运行职责，当前没有充分证据证明其中有可安全移除的冗余内容。日志目录中的大项（如 `link-bilibili-live/`、`link-wechat-live/`、`video-live-verification-*`、`video-cold-diagnostic-*`）与验收/诊断命名一致，保留供追溯；空目录和零字节日志也未擅自删除。

对其他开发计划和历史文档中的旧路径只做了引用检查，没有改写历史记录。现存引用主要描述当时的实现计划或兼容边界；运行入口说明已集中更新在根目录 README、操作手册和本记录中。
